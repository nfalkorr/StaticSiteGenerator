import htmlnode
import textnode

def split_nodes_delimiter(old_nodes,delimiter,text_type):
	#if old node not in texttypes
		#add to new list no modifications
	#else if raise exception invalid markdown syntax
	#.split() by delimiters
	#.extend() each element to the list
	pass
